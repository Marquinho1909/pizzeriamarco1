package sample.functional_logic;

public class ProfileEditModalServiceTest {
    /*
    @Mock
    UserDAO userDAO;

    OrderPositionService service;

    @Before
    public void setUp() {
        MockitoAnnotations.initMocks(this);
        service = new OrderPositionService();
        service.userDAO = userDAO;
    }

    @Test
    public void saveChanges() throws SQLException {
        UserSessionSingleton.currentSession().setUser( new Customer(1, "Debby", "Dummy", new Customer.Address("Dummy Straße", "24", 12345), 'f', "itzDebbyDummy@yahoo.com", "12345"));
        Mockito.when(service.userDAO.get(1)).thenReturn(java.util.Optional.of(new Customer(1, "Debby", "Dummy", new Customer.Address("Dummy Straße", "24", 12345), 'f', "itzDebbyDummy@yahoo.com", "123456")));

        service.saveChanges(1, new Customer(1, "Debby", "Dummy", new Customer.Address("Dummy Straße", "24", 12345), 'f', "itzDebbyDummy@yahoo.com", "123456"));
        Mockito.verify(service.userDAO).update(1, new Customer(1, "Debby", "Dummy", new Customer.Address("Dummy Straße", "24", 12345), 'f', "itzDebbyDummy@yahoo.com", "123456"));
        Assert.assertEquals("123456", UserSessionSingleton.currentSession().getUser().getPassword());
    }

     */
}